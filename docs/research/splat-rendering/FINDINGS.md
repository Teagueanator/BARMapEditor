# Findings — Recoil terrain shader + splat / DNTS composite math

**Status:** Pending sessions.

Two side-by-side files are expected (matching the convention from
`textures/`, `ui/`, `mapinfo/`):

- `claude-research-findings.md` — Claude deep-research session output.
- `gemini-research-findings.md` — Gemini deep-research session output.

Paste each session's output verbatim into the corresponding file once it
returns. Don't edit the originals — corrections live in `NOTES.md` so the
raw output stays auditable.

---

(awaiting research sessions against `PROMPT.md`)
